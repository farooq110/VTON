# Admin Portal environment variables

# Backend API base URL (Express server). Default: http://localhost:4000/api
VITE_API_URL=http://localhost:4000/api

# Enable the marketing/demo route (/demo). Default: true
VITE_DEMO_ENABLED=true

# App display name (shown in sidebar + page titles).
VITE_APP_NAME=Admin Portal
